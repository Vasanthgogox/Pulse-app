// Placeholder shared types entry point for future monorepo use.
// Today, app code imports from '@/types'; when we split into a monorepo,
// types can be moved or re-exported here.

export * from '../../../types';

