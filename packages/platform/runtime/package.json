{
  "name": "@pulse/platform-runtime",
  "version": "0.1.0",
  "private": true,
  "description": "Internal Platform Runtime — executeCommand orchestrates Command Store, Timeline, Events",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@pulse/contracts": "file:../../contracts"
  }
}
