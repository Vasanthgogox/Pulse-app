# Web app (future)

Placeholder for a future web client:

- Intended to live under `apps/web` alongside `apps/mobile`.
- Can consume shared utilities and types from `packages/shared-types` once the monorepo is fully wired.

No implementation yet – this directory exists to make the repository monorepo-ready and to guide future containerization.

