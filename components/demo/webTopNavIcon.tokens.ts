import Theme from "@/constants/Theme";

/** Desktop web top nav — outline utility icons (Metronic reference). */
export const WEB_TOP_NAV_ICON = {
  muted: "#5E6278",
  active: "#181C32",
  onDark: "#ffffff",
  onHover: Theme.networkBadgeClientText,
  stroke: 1.85,
  size: 20,
} as const;
