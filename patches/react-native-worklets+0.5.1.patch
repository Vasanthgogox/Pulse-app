diff --git a/node_modules/react-native-worklets/lib/module/PlatformChecker/index.js b/node_modules/react-native-worklets/lib/module/PlatformChecker/index.js
index ac5be8e..5e0e594 100644
--- a/node_modules/react-native-worklets/lib/module/PlatformChecker/index.js
+++ b/node_modules/react-native-worklets/lib/module/PlatformChecker/index.js
@@ -6,7 +6,15 @@ let IS_JEST = false;
 let IS_WEB = false;
 let IS_WINDOWS = false;
 let SHOULD_BE_USE_WEB = false;
-if (globalThis.__RUNTIME_KIND === RuntimeKind.ReactNative) {
+// Metro's `inlineRequires` (and Jest's eager-import bootstrap) can evaluate
+// this module BEFORE `runtimeKind.js` has had a chance to set
+// `globalThis.__RUNTIME_KIND`. Treat an unset runtime kind as the React Native
+// runtime so the platform checks below run and `SHOULD_BE_USE_WEB` is computed
+// correctly on web (and during SSR via `expo-router/node/render.js`).
+if (
+  globalThis.__RUNTIME_KIND === undefined ||
+  globalThis.__RUNTIME_KIND === RuntimeKind.ReactNative
+) {
   IS_JEST = RN_IS_JEST;
   IS_WEB = RN_IS_WEB;
   IS_WINDOWS = RN_IS_WINDOWS;
diff --git a/node_modules/react-native-worklets/src/PlatformChecker/index.ts b/node_modules/react-native-worklets/src/PlatformChecker/index.ts
index 3433040..16e4d28 100644
--- a/node_modules/react-native-worklets/src/PlatformChecker/index.ts
+++ b/node_modules/react-native-worklets/src/PlatformChecker/index.ts
@@ -13,7 +13,15 @@ let IS_WEB = false;
 let IS_WINDOWS = false;
 let SHOULD_BE_USE_WEB = false;
 
-if (globalThis.__RUNTIME_KIND === RuntimeKind.ReactNative) {
+// Metro's `inlineRequires` (and Jest's eager-import bootstrap) can evaluate
+// this module BEFORE `runtimeKind.ts` has had a chance to set
+// `globalThis.__RUNTIME_KIND`. Treat an unset runtime kind as the React Native
+// runtime so the platform checks below run and `SHOULD_BE_USE_WEB` is computed
+// correctly on web (and during SSR via `expo-router/node/render.js`).
+if (
+  globalThis.__RUNTIME_KIND === undefined ||
+  globalThis.__RUNTIME_KIND === RuntimeKind.ReactNative
+) {
   IS_JEST = RN_IS_JEST;
   IS_WEB = RN_IS_WEB;
   IS_WINDOWS = RN_IS_WINDOWS;
