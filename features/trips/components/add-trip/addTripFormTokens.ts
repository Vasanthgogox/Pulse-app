/**
 * Shared Create Trip form density — one scale for labels, inputs, and money rows.
 */
export const ADD_TRIP_FORM = {
  labelSize: 10,
  labelLine: 13,
  labelSpacing: 5,
  fieldHeight: 46,
  fieldRadius: 12,
  fieldPadH: 12,
  fieldPadV: 10,
  fieldFontSize: 14,
  fieldLineHeight: 20,
  fieldGap: 5,
  cardPad: 14,
  cardGap: 10,
  moneyIconSize: 16,
} as const;
