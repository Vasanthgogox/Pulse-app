/**
 * Chat feature accent — same hue as outgoing message bubbles (`bubbleOwn`).
 * Use across `features/chat` instead of generic theme blue / older violet tokens.
 */
export const CHAT_ACCENT = "#5b5ef4";
export const CHAT_ACCENT_SOFT = "#eef2ff";
/** Light borders / dividers (indigo tint aligned to CHAT_ACCENT) */
export const CHAT_ACCENT_BORDER = "#c7d2fe";
/** Unselected icons on light pills (party row, inactive main tabs) */
export const CHAT_ICON_MUTED = "#475569";
