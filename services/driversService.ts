/**
 * Public drivers service facade for the mobile app.
 * Re-exports the feature-level drivers service so callers can use `@/services/driversService`.
 */
export * from '@/features/drivers/services/drivers.service';

